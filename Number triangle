function numericalTriangle(n){
 //function numericalTriangle(n){
  let str = '';
    for(let i = 1; i <= n; i++) {
    for(let j = 1; j <= i; j++) {
      if (j < i) str += i + ' ';
      else str += i;
  }
      if (i !== n) str += '\n';
 }
  return str;
  }
