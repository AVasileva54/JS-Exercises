//Write a function includesOtherMax that takes two numeric arrays arr1 and arr2 as arguments and returns true if arr1 contains the maximum element of arr2, and false otherwise.

const includesOtherMax = (arr1, arr2) => (arr1.includes(Math.max(...arr2)))
