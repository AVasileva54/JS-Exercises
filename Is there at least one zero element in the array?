//Write a function includeZero that takes the arr array as an argument and returns true if the array contains at least one element equal to 0, and false otherwise.

function includeZero(arr){
return (arr.includes(0))
}
