function numericalTriangle(n){
  let str = '';
    for(let i = 1; i <= n; i++) {
    for(let j = 1; j <= i; j++) {
      if (j < i) str += '*';
      else str += '*';
  }
      if (i !== n) str += '\n';
 }
  return str;
