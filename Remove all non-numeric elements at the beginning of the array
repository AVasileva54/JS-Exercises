function deleteNotNumbers(arr){
let notNum = 0;
  for(let i = 0; i <= arr.length; i++){
    if(typeof arr[i] !== 'number'){
      notNum++;
      } else break;
    }
for(let i = 0; i < notNum; i++){
  arr.shift();
}
return arr
}
